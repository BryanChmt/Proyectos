import 'package:flutter/material.dart';

class Minar extends StatelessWidget {
  const Minar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Minería')),
    );
  }
}